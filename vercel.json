{
  "buildCommand": "node -e \"require('child_process').execSync(require('path').join('node_modules','.bin','vite')+' build',{stdio:'inherit'})\"",
  "outputDirectory": "dist",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
